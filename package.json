{
  "name": "cherry-noir",
  "version": "1.0.0",
  "description": "Cherry Noir — The Darkest Cherry. The Finest Pour.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@studio-freight/lenis": "^1.0.42",
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.37.2",
    "vite": "^6.4.2",
    "wrangler": "^4.93.0"
  }
}
